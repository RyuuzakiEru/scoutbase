# import * from './generated/prisma.graphql'

type Mutation {
  createUser(name: String!, password: String!): UserAuth!
}

type Query {
  hi: String
}

type User{
  id: ID!
  name: String!
}

type UserAuth {
  token: String!
  user: User!
}